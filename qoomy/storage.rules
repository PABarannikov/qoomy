rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Room images - only authenticated users can upload
    match /rooms/{roomCode}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
